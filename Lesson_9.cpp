/* Lesson on Math related functions */
#include <iostream>
#include <cmath>



int main() {

    using namespace std;

    double x = 3.1;
    double y = 4;
    double z;

    // z = max(x , y);
    // z = min(x , y);
    // z = pow(2 , 4);
    // z = sqrt(9);
    // z = abs(-3); /* Absoulute value function */
    // z = round(x);
    z = ceil(x);

    cout << z << endl;

    return 0;
}